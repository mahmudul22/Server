Title: README
1. First run the pyro naming server with the following command:
		$ python -m Pyro4.naming
		
2. Now run the server module by executing the following command:
		$ python server.py

3. Finally run the client module using the following lines:
 		$ python client.py